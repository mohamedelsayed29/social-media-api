export enum SignatureLevelEnum{
    Bearer = "Bearer",
    System = "System"
}

export enum TokenTypeEnum{
    access = "access",
    refresh = "refresh"
}

export enum LogoutEnum{
    only = "only",
    all = "all"
}